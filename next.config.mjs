/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upload.wikimedia.org",
      "miro.medium.com",
      "www.svgrepo.com",
      "encrypted-tbn0.gstatic.com",
    ],
  },
};

export default nextConfig;
